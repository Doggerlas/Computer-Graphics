二值图：0黑 1白
2dmask 		黑底 白色区域填充为轮廓线包围区域 边缘比较平滑 																						草图轮廓遮罩：背景为0 前景为1
shapemask 	黑底 白色区域填充为轮廓线包围区域 比2dmask瘦一圈 而且边缘比较尖锐																	形状轮廓掩码：比2dmask缩小了一个小边距 背景为0 前景为1
clmask 		白底黑线 描述图形边沿(不仅仅是轮廓线) 部分内部也有笔划																				轮廓线：轮廓为0，非轮廓为1
dsMask		黑底白线 ds图的二值图																												深度轮廓：1为深度 0为非深度
fLInvMask	白底黑线 功能未知																													信号过滤掩码  去除不连续（遮挡）轮廓像素处的无效信号、锐利特征（如果有）和背景 0为去除 1为保留
fLMask		黑底白线 功能未知 是fLInvMask的互补图 fLInvMask与fLMask不是每个图像都有的 应该是可选的												曲率提示通道1 对于锐利特征的像素为 1，否则为 0
slmask		黑底白线 功能未知 不是每个图像都有的 应该是可选的																					曲率提示通道2 指示存在目标曲率 1为存在 0不存在

灰度图：计划用子菜单分为五个灰度选项实现	
curMag 		黑底灰线 用以描述目标区域曲率  																										曲率提示通道3 灰度代表笔划上的像素目标曲率值
ds			黑底灰线 用以描述目标区域深度																										深度样本 在某些点提供深度提示 灰度代表样本点的像素具有的深度值
npr			白底灰线 非真实感渲染 默认用模板给出  显示是在opengl的交互窗口以二值化显示(不二值化显示会出雪花) 提供灰度选项 可以自加灰度线		纯黑是轮廓 灰度是脊/谷、暗示轮廓、曲率线等
